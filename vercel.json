{
  "version": 2,
  "buildCommand": "npm install && npm run build --prefix apps/web/datingapp-web",
  "outputDirectory": "apps/web/datingapp-web/dist",
  "framework": "vite",
  "env": {
    "NODE_ENV": "production"
  }
}
