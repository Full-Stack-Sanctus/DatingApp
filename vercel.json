{
  "version": 2,
  "buildCommand": "cd apps/web/datingapp-web && npm install && npm run build",
  "outputDirectory": "apps/web/datingapp-web/dist",
  "framework": "vite",
  "env": {
    "NODE_ENV": "production"
  }
}
